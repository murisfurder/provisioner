- name: Provisions Redmine (running inside Docker)
  hosts: all
  sudo: yes

  vars:
    - mysql_root_password:
    - http_port: 8003
  roles:
    - unattended-upgrades
    - docker.ubuntu
    - docker_network
    - mysql
    - redmine

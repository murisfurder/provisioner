- name: Provisions Docker Registry (running inside Docker)
  hosts: all
  sudo: yes

  vars:
    - http_port: 5000

  roles:
    - unattended-upgrades
    - docker.ubuntu
    - docker_network
    - nginx
    - letsencrypt
    - docker_registry
